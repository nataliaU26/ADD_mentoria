// src/authConfig.js
export const msalConfig = {
    auth: {
        clientId: "Tu Application (client) ID aquí",
        authority: "https://login.microsoftonline.com/Tu Directory (tenant) ID aquí",
        redirectUri: "http://localhost:3000"
    }
};
